services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.dev
    env_file:
      - .env
    volumes:
      - ${USERPROFILE}/.aws:/root/.aws:ro
      - ./backend:/app
    ports:
      - "8000:8000"
