services:
  backend:
    build:
      context: .
      dockerfile: backend/dockerfile
    env_file:
      - ./backend/.env
    volumes:
      - ${USERPROFILE}/.aws:/root/.aws:ro
    networks:
      - pcs_net

  frontend:
    build:
      context: .
      dockerfile: frontend/dockerfile
    depends_on:
      - backend
    ports:
      - "3000:80"
    networks:
      - pcs_net

networks:
  pcs_net:
    driver: bridge
